# environment management
#   micromamba self-update
#   micromamba env create -f mamba-cad.yaml
#   micromamba env export --from-history
#   micromamba env export

# build123d ocpsvg
#   mamba activate cad
#   pip install --no-deps build123d ocpsvg

name: cad
channels:
  - conda-forge
dependencies:
  - python
  - pip
  - jupyterlab
  - xonsh
  - pip:
    - build123d
