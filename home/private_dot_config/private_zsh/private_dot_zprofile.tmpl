# -- .zprofile ----------------------------------------------------------------

typeset -gU cdpath fpath path

{{ if stat "/opt/homebrew/bin/brew" -}}
eval "$(/opt/homebrew/bin/brew shellenv)"
{{ end -}}
